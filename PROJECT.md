# PROJECT.md — HRSG 가스측 오염도 진단 및 세정 시점·편익 산출 AI 웹앱

> 이 문서는 프로젝트의 **단일 진실 공급원(Single Source of Truth)** 개요다.
> 세부 요구사항은 `specs/` 폴더의 개별 명세를 따른다.
> 코드 작성 규칙과 에이전트 작업 지침은 `AGENTS.md`를 따른다.

---

## 1. 개요

### 1.1 프로젝트명
HRSG 가스측 오염도 진단 및 세정 시점·편익 산출 AI 웹앱
(코드네임: **HRSG Fouling Advisor**)

### 1.2 배경
복합화력 발전소의 HRSG(배열회수보일러) 가스측 전열면은 운전 시간이 누적될수록 회분·미연분·부식 생성물 등이 부착되어 오염(fouling)이 진행된다.
오염이 진행되면 다음과 같은 손실이 발생한다.

- 가스측 차압 상승 → GT 배압 상승 → **GT 출력 감소 및 열소비율 악화**
- 전열면 열전달 저하 → **스택 온도 상승 → 배열회수량 감소 → ST 출력 감소**

현재 현장에서는 세정 시점을 **담당자의 경험과 고정된 정비 주기**에 의존해 결정한다.
그 결과 (a) 너무 이른 세정으로 불필요한 정지 손실이 생기거나, (b) 너무 늦은 세정으로 출력 손실이 누적되는 문제가 반복된다.

### 1.3 목적
운전 데이터로부터 **오염도 지수(Fouling Index, FI)** 를 자동 산출하고,
**임계치 도달 예상일(D-day)** 과 **세정 시 예상 회수 편익**을 정량적으로 제시하여
세정 의사결정을 경험 기반에서 **데이터 기반**으로 전환한다.

### 1.4 목표 (Success Criteria)
| # | 목표 | 측정 지표 |
|---|------|-----------|
| G1 | 오염도의 정량화 | 운전 조건 보정 후 FI 시계열이 세정 이벤트에서 뚜렷이 회복(하락)하는 패턴을 재현 |
| G2 | 기대값 예측 정확도 | 청정 기준 기간 검증셋에서 차압 R² ≥ 0.85, 스택온도 MAE ≤ 3 ℃ (목표치, 설정 가능) |
| G3 | 세정 시점 제시 | 임계치 도달 예상일을 신뢰구간과 함께 제시, 백테스트로 오차 검증 |
| G4 | 편익 정량화 | 세정 순편익(원), 회수기간(일), 세정 지연 시 일일 손실(원/일) 산출 |
| G5 | 보고 활용성 | 한글 깨짐 없는 PDF/엑셀 리포트를 클릭 3회 이내로 생성 |

### 1.5 범위
**In scope**
- 사내 사용자 로그인 및 권한 분리(일반 사용자 / 관리자)
- 호기별 운전 데이터 CSV 업로드·검증·누적 저장
- 데이터 정제, 운전 구간 필터링, 부하대/계절 군집화
- 운전 조건 보정 기대값 예측 모델(차압·스택온도)
- 오염도 지수 산출, 등급 판정, 추세 예측, D-day 산출
- 세정 편익 산출 및 민감도 확인
- 대시보드 시각화, 세정 전후 비교 리포트, PDF/엑셀 다운로드
- 정비 이력 업로드 및 오염 관련 이력 자동 추출
- 관리자 설정(임계치·가중치·편익 계수·컬럼 매핑·키워드 사전 등)
- 가상 샘플 데이터 생성 스크립트

**Out of scope (본 프로젝트에서 하지 않음)**
- DCS/PI 등 실시간 시스템과의 자동 연계(수집은 CSV 업로드 기반)
- 세정 작업 자체의 제어·자동화
- 가스터빈 압축기 오염(compressor fouling) 진단
- 모바일 전용 앱, 외부 공개 서비스
- 다국어 지원(한국어 UI 단일)

### 1.6 사용자
| 구분 | 설명 | 주 사용 시나리오 |
|------|------|------------------|
| 일반 사용자 | 발전소 운영·정비 담당자 | 월 1~4회, 호기별 신규 운전 데이터 업로드 → 분석 실행 → 대시보드 확인 → 보고용 리포트 다운로드 |
| 관리자 | 성능관리 담당자 / 시스템 관리자 | 사용자·호기 관리, 컬럼 매핑, 임계치·가중치·편익 계수 설정, 세정 이력 등록, 모델 재학습 |

---

## 2. 아키텍처

```
┌──────────────────────────────────────────┐
│  Vue.js 3 SPA (Vite + Bootstrap 5)       │
│  - 로그인 / 대시보드 / 업로드 / 분석      │
│  - 리포트 / 관리자 콘솔                   │
└──────────────────┬───────────────────────┘
                   │ HTTP / JSON (REST, JWT or Session)
                   v
┌──────────────────────────────────────────┐
│  Django + Django REST Framework          │
│  - 인증/권한, 업로드 검증, 분석 오케스트레이션 │
│  - 분석 엔진 (pandas, scikit-learn)       │
│  - 리포트 생성 (openpyxl, PDF)            │
└──────────────────┬───────────────────────┘
                   │ Django ORM
                   v
┌──────────────────────────────────────────┐
│  PostgreSQL                              │
│  - 사용자/호기/운전데이터/설정/분석결과      │
└──────────────────────────────────────────┘
```

### 2.1 기술 스택
| 레이어 | 기술 |
|--------|------|
| Frontend | Vue.js 3 (Composition API), Vite, Vue Router, Pinia, Bootstrap 5, HTML5, CSS3, Chart 라이브러리 |
| Backend | Python 3.11+, Django 5.x, Django REST Framework, Django ORM |
| 분석 | pandas, numpy, scikit-learn, scipy |
| 리포트 | openpyxl(엑셀), ReportLab 또는 WeasyPrint(PDF, 한글 폰트 임베딩) |
| DB | PostgreSQL 14+ |
| 기타 | python-dotenv, django-cors-headers, pytest/pytest-django, Vitest |

### 2.2 핵심 데이터 흐름
```
CSV 업로드
  → 컬럼 매핑 적용(호기별) → 스키마 검증 → 원본 적재(RawMeasurement)
  → 정제(결측/이상치) → 운전 구간 필터(기동·정지·부하급변·덕트버너 제외)
  → 부하대/계절 군집화
  → 기대값 예측(차압, 스택온도) : 청정 기준 기간 학습 모델
  → 잔차 → 정규화·가중합 → 오염도 지수 FI(0~100)
  → 등급 판정 / 추세 회귀 → 임계치 도달 D-day
  → 편익 산출(전력단가·세정비용·정지일수 등)
  → 대시보드 + PDF/엑셀 리포트 + 분석 이력 저장
```

---

## 3. 도메인 용어집 (Glossary)

| 용어 | 설명 |
|------|------|
| HRSG | Heat Recovery Steam Generator, 배열회수보일러 |
| GT / ST | Gas Turbine / Steam Turbine |
| 호기(Unit) | 분석 단위가 되는 개별 설비(예: 1호기 HRSG) |
| 가스측 차압(dP) | HRSG 가스 입구-출구 간 압력 손실. 오염 진행 시 상승 |
| GT 배압 | GT 배기 측 압력. 차압 계측이 없을 때 대체 지표로 사용 |
| 스택 온도 | HRSG 출구 배기가스 온도. 열전달 저하 시 상승 |
| 덕트버너 | HRSG 보조 연소기. 가동 구간은 열/유동 조건이 달라 분석에서 제외 |
| IGV 개도 | GT 압축기 입구 안내익 개도. 배기유량 대체 지표 |
| 청정 기준 기간 (Clean Baseline) | 세정 직후 오염이 없다고 간주하는 구간. 기대값 모델 학습 구간 |
| 기대값 (Expected Value) | 동일 운전 조건에서 청정 상태라면 나올 차압/스택온도 예측값 |
| 잔차 (Residual) | 실측값 − 기대값. 오염의 직접 신호 |
| 오염도 지수 (FI) | 잔차를 정규화·가중합해 0~100으로 환산한 지수 |
| 임계치 (Threshold) | 세정 권고 기준이 되는 FI 값(기본 60) |
| D-day | FI 추세가 임계치에 도달할 것으로 예측되는 날짜까지 남은 일수 |
| 회수 편익 (Recovery Benefit) | 세정으로 회복되는 출력·연료 이득에서 세정 비용·정지 손실을 뺀 순편익 |
| 백테스트 | 과거 세정 시점을 기준으로 모델의 D-day 예측 정확도를 검증하는 절차 |

---

## 4. 명세 문서 목록 (`specs/`)

| 파일 | 내용 |
|------|------|
| `specs/00-requirements-index.md` | 전체 요구사항 인덱스 및 추적 매트릭스 |
| `specs/01-auth-and-users.md` | 로그인, 권한, 사용자 관리, 기본 관리자 계정 |
| `specs/02-unit-and-column-mapping.md` | 호기 관리, 컬럼 매핑 |
| `specs/03-data-ingestion.md` | 운전 데이터 CSV 업로드, 검증, 누적 저장 |
| `specs/04-preprocessing.md` | 결측/이상치 정제, 운전 구간 필터, 분석 기간 선택 |
| `specs/05-clustering.md` | 부하대/계절별 운전 구간 군집화 |
| `specs/06-expected-value-model.md` | 기대 차압·기대 스택온도 예측 모델, 정확도 지표 |
| `specs/07-fouling-index.md` | 오염도 지수 산출, 등급 판정, 가중치 |
| `specs/08-trend-and-dday.md` | 추세 예측, 임계치 도달 예상일 |
| `specs/09-benefit-model.md` | 세정 편익 산출 모델 |
| `specs/10-maintenance-history.md` | 정비 이력 업로드, 오염 키워드 추출, 세정 이력 |
| `specs/11-dashboard.md` | 대시보드 화면 및 시각화 |
| `specs/12-reports.md` | 세정 전후 비교 리포트, PDF/엑셀 출력 |
| `specs/13-admin.md` | 관리자 기능 전체 |
| `specs/14-data-model.md` | DB 모델(ERD) 정의 |
| `specs/15-api.md` | REST API 명세 |
| `specs/16-frontend.md` | 프론트엔드 화면·라우팅·상태관리 |
| `specs/17-sample-data.md` | 가상 샘플 데이터 생성 스크립트 |
| `specs/18-nonfunctional.md` | 비기능 요구사항(보안, 성능, 로깅, 배포) |
| `specs/19-optional-features.md` | 옵션 기능(자동 재계산, 백테스트, 호기 간 비교) |

---

## 5. 디렉터리 구조 (계획)

```
hrsg_fouling_advisor/
├── AGENTS.md
├── PROJECT.md
├── specs/
│   └── *.md
├── backend/
│   ├── manage.py
│   ├── requirements.txt
│   ├── config/                 # Django 프로젝트 설정
│   │   ├── settings/           # base.py / dev.py / prod.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── accounts/               # 사용자, 인증 (AbstractUser 확장)
│   ├── units/                  # 호기, 컬럼 매핑, 설정값
│   ├── ingestion/              # CSV 업로드, 검증, 원본 적재
│   ├── analysis/               # 정제, 군집화, 모델, FI, 추세, 편익
│   │   ├── models.py
│   │   ├── services/           # 순수 파이썬 분석 로직
│   │   │   ├── cleaning.py
│   │   │   ├── segmentation.py
│   │   │   ├── clustering.py
│   │   │   ├── expected_model.py
│   │   │   ├── fouling_index.py
│   │   │   ├── trend.py
│   │   │   └── benefit.py
│   │   └── tasks.py
│   ├── maintenance/            # 정비 이력, 세정 이력, 키워드 사전
│   ├── reports/                # PDF / 엑셀 생성
│   │   └── fonts/              # 한글 폰트(NanumGothic 등)
│   ├── common/                 # 공통 유틸, 예외, 페이지네이션
│   └── scripts/
│       └── generate_sample_data.py
├── frontend/
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── router/
│       ├── stores/
│       ├── api/
│       ├── components/
│       ├── views/
│       └── assets/
└── docs/                       # 산출물, 다이어그램 (선택)
```

---

## 6. 개발 마일스톤

| 단계 | 내용 | 산출물 |
|------|------|--------|
| M0 | 명세 확정 | `AGENTS.md`, `PROJECT.md`, `specs/*.md` |
| M1 | 기반 구축 | Django 프로젝트, PostgreSQL 연결, 커스텀 User, Vue SPA 스캐폴드, 로그인 |
| M2 | 데이터 파이프라인 | 호기/컬럼 매핑, CSV 업로드·검증·적재, 샘플 데이터 생성 스크립트 |
| M3 | 분석 엔진 | 정제, 구간 필터, 군집화, 기대값 모델, FI 산출 |
| M4 | 의사결정 지원 | 추세·D-day, 편익 산출, 대시보드 |
| M5 | 이력·리포트 | 정비/세정 이력, 전후 비교, PDF/엑셀 출력 |
| M6 | 관리자 콘솔 | 설정·사용자·호기·키워드·재학습·분석 이력 |
| M7 | 옵션 기능 | 자동 재계산, 백테스트, 호기 간 비교 |
| M8 | 안정화 | 테스트, 성능, 보안 점검, 배포 문서 |

---

## 7. 가정 및 리스크

| 항목 | 내용 | 대응 |
|------|------|------|
| 가정 | 호기별 운전 데이터는 최소 1분~10분 주기로 제공된다 | 리샘플링 주기를 호기 설정값으로 둔다 |
| 가정 | 최소 1회 이상의 과거 세정 이력이 존재하거나 관리자가 청정 기준 기간을 지정할 수 있다 | 없을 경우 데이터 초기 구간을 기본 청정 기준으로 사용하고 경고를 표시 |
| 리스크 | 차압 계측기가 없는 호기 존재 | GT 배압 대체, 스택온도 가중치 상향(설정 가능) |
| 리스크 | 계측기 드리프트가 오염으로 오인될 수 있음 | 급격한 단차(step change) 탐지 시 경고 배지 표시 |
| 리스크 | 청정 기준 기간이 짧아 모델 과적합 | 학습 샘플 수 하한 체크 및 경고, 정규화 모델 사용 |
| 리스크 | 한글 PDF 폰트 라이선스 | 오픈 라이선스 폰트(나눔고딕 등) 동봉 |
